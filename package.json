{
  "name": "token-manager-server",
  "version": "0.1.2",
  "description": "HTTP Server to handle authorization tokens",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "token",
    "token-manager",
    "server",
    "authorization"
  ],
  "bin": "./bin/runserver",
  "author": "Jeferson Sanches",
  "repository": "https://github.com/jsanchesleao/token-manager-server.git",
  "license": "ISC",
  "dependencies": {
    "token-manager": "0.1.1",
    "restify": "~2.6.1"
  }
}
